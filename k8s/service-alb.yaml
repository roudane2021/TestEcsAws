---
# Service ClusterIP (à utiliser avec Ingress ALB)
apiVersion: v1
kind: Service
metadata:
  name: my-api-service
  labels:
    app: my-api
spec:
  type: ClusterIP
  selector:
    app: my-api
  ports:
    - port: 80
      targetPort: 8081
      protocol: TCP
